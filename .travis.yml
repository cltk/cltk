language: python

python:
  - "3.4"

install:
  # - pip install cltk
  # Note: requests must be installed before `python setup.py sdist install`.
  - pip install requests
  - python setup.py sdist install

script:
  - cd cltk/tests/
  - python test_cltk.py
  # TODO: add nose + pylint check
