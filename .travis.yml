sudo: required  # install TLGU w/o sudo and then use `false`

language: python

python:
  - "3.4"
  - "3.5"

install:
  # openblas req'd for numpy
  - sudo apt-get install -y libblas-dev liblapack-dev
  # fortran for scipy
  # TODO: trim this down
  - sudo apt-get install -y libamd2.2.0 libblas3gf libc6 libgcc1 libgfortran3 liblapack3gf libumfpack5.4.0 libstdc++6 build-essential gfortran python-all-dev libatlas-base-de
  - pip install gensim
  - python setup.py sdist install

before_script:
  - pip install codecov
  - pip install coveralls

script:
  # Notes on nose:
  # Travis CI pre-installs `nose`
  # https://github.com/coagulant/coveralls-python#nosetests
  # http://nose.readthedocs.org/en/latest/plugins/skip.html
  - nosetests --no-skip --with-coverage --cover-package=cltk

after_success:
  - codecov
  # Notes on Coveralls
  # https://github.com/coagulant/coveralls-python#usage-travis-ci
  - coveralls
